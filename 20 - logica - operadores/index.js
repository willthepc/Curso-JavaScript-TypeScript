/*
Operadores de comparaçõo

> Maior que
>= Maior que ou Igual a
< Menor que
<= Menor que
== Igualdade (valor) -> Não recomendado
=== Igualdade estrita (valor e tipo)
!= Diferente (valor) -> Não recomendado
!== Diferente estrito (valor e tipo)
 */

const num1 = 10;
const num2 = 10;
const comp = num1 === num2;
console.log(comp);